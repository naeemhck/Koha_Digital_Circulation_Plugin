use Modern::Perl; use Test::More; use lib '.'; use Koha::Plugin::Com::JunaidZaidiLibrary::DigitalCirculation::Service::StateMachine qw(can_transition);
for(qw(APPROVED REJECTED CANCELLED)){ok can_transition('request','PENDING',$_),"request PENDING -> $_"}for(qw(REJECTED APPROVED)){ok !can_transition('request','APPROVED',$_),"forbidden approved -> $_"}for(qw(RENEWAL_PENDING RETURNED EXPIRED REVOKED)){ok can_transition('loan','ACTIVE',$_),"loan ACTIVE -> $_"}ok can_transition('loan','RENEWAL_PENDING','ACTIVE'),'renewal decision returns active';for(qw(RETURNED EXPIRED REVOKED)){ok !can_transition('loan',$_,'ACTIVE'),"terminal $_"}for(qw(APPROVED REJECTED CANCELLED)){ok can_transition('renewal','PENDING',$_),"renewal PENDING -> $_"}done_testing;
